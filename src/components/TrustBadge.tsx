function TrustBadge() {
  return (
    <span
      className="glass-pill"
      style={{ padding: '0.35rem 0.75rem', color: 'var(--success-text)', background: 'var(--success-soft)' }}
    >
      FBR compliant
    </span>
  )
}

export default TrustBadge
